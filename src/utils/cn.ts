/**
 * @module utils/cn
 * @description إعادة تصدير دالة `cn()` من `lib/utils` لتوحيد مسار الاستيراد.
 * تجمع بين `clsx` و `tailwind-merge` لدمج أصناف CSS بذكاء.
 */
export { cn } from '../lib/utils'

