# قائمة المهام

## 🧪 الاختبارات (Testing)

### تغطية مسارات الاستيراد والتحرير
- [ ] إنشاء اختبارات وحدة (unit) لمسارات الاستيراد/التحرير
- [ ] إنشاء اختبارات تكامل (integration) لـ `file-extraction` و `file-open-pipeline`
- [ ] إضافة اختبارات انحدار (regression) لعمليات import في `EditorArea`

### Test Harness
- [ ] بناء test harness لـ `structure-pipeline`
- [ ] بناء test harness لـ `plain-text-to-blocks`
- [ ] بناء test harness لـ `open-pipeline` مع integration smoke على تدفق الاستيراد

### استيراد الملفات
- [ ] إضافة/تعزيز اختبارات regression لاستيراد DOC/DOCX/PDF
- [ ] ضمان عدم حدوث تراجعات متكررة في معالجة الملفات

---

## 🔧 البنية التحتية (Infrastructure)

### API & Backend
- [ ] دمج مسارات review/extract داخل نفس runtime
- [ ] إنشاء routes مكافئة لـ `/api/files/extract` و `/api/agent/review`
- [ ] إضافة عقود تحقق أقوى لطلب/استجابة `agent-review` و `extract`
- [ ] الحفاظ على نموذج backend المنفصل الحالي

### Hardening & Contracts
- [ ] نقل/إعادة تنفيذ أجزاء hardening من `file-extraction` و `file-open-pipeline`
- [ ] توافق كامل مع عقود `types/file-import.ts`

### Logger & Telemetry
- [ ] تبنّي أجزاء مناسبة من `src/utils/logger.ts`

---

## 📦 الميزات (Features)

### التصدير
- [ ] بناء export layer مستقلة لتصدير PDF/DOCX
- [ ] اتباع نهج مشابه لـ `src/utils/exporters.ts`

### AI & ML (تقييم)
- [ ] تقييم تبنّي AI flows من `src/ai/*`
- [ ] تقييم تبنّي ML local من `src/ml/*`
- [ ] انتظار توضيح احتياج المنتج قبل التنفيذ

### Utilities إضافية (تقييم)
- [ ] تقييم `storage.ts`
- [ ] تقييم `typing-workflow-rules.ts`
- [ ] تقييم `context-window.ts`

---

## 🏗️ الهيكلة المعمارية (Architecture)

### App Shell ✅
- [x] نقل تفكيك shell إلى مكونات مستقلة:
  - `src/components/app-shell/AppHeader.tsx`
  - `src/components/app-shell/AppSidebar.tsx`
  - `src/components/app-shell/AppDock.tsx`
  - `src/components/app-shell/AppFooter.tsx`

### القوائم والأوامر
- [ ] استكمال فصل منطق القوائم/الأوامر
- [ ] نقل إلى طبقات `hooks/controllers` مستقلة

---

## 📝 ملاحظات

- المهام المكتملة تُشطب بوضع `[x]`
- المهام ذات الأولوية المنخفضة مُدرجة تحت "تقييم"
